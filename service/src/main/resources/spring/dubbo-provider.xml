<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="dubbo-demo-service" />

    <!-- 使用Nacos作为注册中心 -->
    <dubbo:registry address="nacos://nacos-server:8848" />

    <!-- 用Dubbo协议在50051端口暴露服务 -->
    <dubbo:protocol name="tri" port="50051" />

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.pocrd.service_demo.api.GreeterServiceHttpExport" 
                   ref="greeterService" 
                   version="1.0.0" 
                   group="default" />

    <!-- 暴露内部服务接口（仅供内部 Dubbo RPC 调用，不通过 Higress 网关暴露） -->
    <dubbo:service interface="com.pocrd.service_demo.api.GreeterServiceInternal" 
                   ref="greeterServiceInternal" 
                   version="1.0.0" 
                   group="internal" />

    <!-- HttpExport 实现 -->
    <bean id="greeterService" class="com.pocrd.service_demo.service.impl.GreeterServiceHttpExportImpl" />

    <!-- Internal 实现 -->
    <bean id="greeterServiceInternal" class="com.pocrd.service_demo.service.impl.GreeterServiceInternalImpl" />

</beans>