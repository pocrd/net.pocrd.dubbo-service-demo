<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo
       http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="dubbo-demo-service" />

    <!-- 使用Nacos作为注册中心 -->
    <dubbo:registry address="nacos://nacos-server:8848" />

    <!-- 用Dubbo协议在50051端口暴露服务 -->
    <dubbo:protocol name="tri" port="50051" />

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.pocrd.service_demo.api.GreeterService" 
                   ref="greeterService" 
                   version="1.0.0" 
                   group="default" />

    <!-- 和本地bean一样实现服务 -->
    <bean id="greeterService" class="com.pocrd.service_demo.service.impl.GreeterServiceImpl" />

</beans>