# 测试环境 Docker Compose 配置
# 使用方式: ./deploy.sh test
# 环境变量从 .env.test 文件加载
#
# Nacos 和 MySQL 运行在 k8s 中，通过宿主机 NodePort 访问
# macOS/Windows 下 host.docker.internal 直接可用，Linux 需要额外配置

services:
  dubbo-service:
    env_file:
      - .env.test
